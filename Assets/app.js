(()=>{'use strict';const text=(node,value)=>{node.textContent=String(value??'')};class DispatchApp{constructor(root,transport){this.root=root;this.transport=transport;this.layers=new Map;root.classList.add('bdvm-dispatch');this.map=document.createElement('div');this.map.className='bdvm-dispatch__map';this.status=document.createElement('p');this.status.className='bdvm-dispatch__status';this.controls=document.createElement('div');this.controls.className='bdvm-dispatch__controls';root.append(this.map,this.status,this.controls);for(const key of ['tracks','junctions','signals','occupations','trains'])this.addToggle(key)}addToggle(key){const label=document.createElement('label'),input=document.createElement('input');input.type='checkbox';input.checked=true;input.onchange=()=>{const layer=this.layers.get(key);if(layer)layer.hidden=!input.checked};label.append(input,document.createTextNode(` ${key}`));this.controls.append(label)}async refresh(){text(this.status,'Loading');try{const state=await this.transport.snapshot();this.render(state);text(this.status,`Online — ${state.correlationId??''}`)}catch(error){text(this.status,`Offline — ${error.message}`)}}render(state){for(const key of ['tracks','junctions','signals','occupations','trains']){let layer=this.layers.get(key);if(!layer){layer=document.createElement('div');layer.className='bdvm-dispatch__layer';layer.dataset.layer=key;this.layers.set(key,layer);this.map.append(layer)}layer.replaceChildren();for(const feature of state[key]||[]){const marker=document.createElement('span');marker.dataset.id=String(feature.id??'');marker.dataset.state=String(feature.state??'');marker.title=String(feature.label??feature.id??'');layer.append(marker)}}}async submit(intentType,payload){const envelope={schemaVersion:1,moduleId:'BDVM.Dispatch',intentType,correlationId:crypto.randomUUID(),idempotencyKey:crypto.randomUUID(),expectedVersion:0,payload};return this.transport.intent(envelope)}}globalThis.BdvmDispatch=Object.freeze({DispatchApp,create(root,transport){return new DispatchApp(root,transport)}})})();
